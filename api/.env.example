# Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=pos
DB_USER=root
DB_PASS=

# Server
SERVER_PORT=8080

# JWT — REQUIRED: must be set to a strong random value (min 32 chars)
# Generate with: openssl rand -base64 48
JWT_SECRET=

# CORS — comma-separated allowed origins
CORS_ORIGINS=http://localhost:8000,http://localhost:8080
